<Window x:Class="VisualControls.ModalContentHandler.ModalDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ModalDialog" Height="300" Width="300">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Themes/Generic.xaml" />
                <ResourceDictionary Source="../Themes/NamedResources.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ContentPresenter Grid.Row="0"
                          Content="{Binding}">

        </ContentPresenter>
        <Border Grid.Row="1" Style="{StaticResource ResourceKey=DialogWindowBottomPanel}">
            <StackPanel Orientation="Horizontal" FlowDirection="RightToLeft">
                <Button Content="Exit" Click="Button_Exit_Click" />
                <Button Content="Cancel" Click="Button_Cancel_Click" />
                <Button Content="Save" Click="Button_Save_Click" />
                <Button Content="OK" Click="Button_Ok_Click" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
